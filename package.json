{
  "name": "sandbox",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "astro build && wrangler deploy",
    "test:heat": "node wasm/test/golden.mjs && node wasm/test/integration.mjs",
    "test:active-tiles": "node test/run-active-tiles.mjs",
    "test": "npm run test:heat && npm run test:active-tiles"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/svelte": "^9.0.1",
    "astro": "^7.0.6",
    "nanostores": "^1.4.0",
    "svelte": "^5.56.4",
    "terser": "^5.49.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.107.0"
  }
}
