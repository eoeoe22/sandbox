# 이 프로젝트는 웹 브라우저 기반 샌드박스 파티클 시뮬레이터임.
1순위 : 재미
2순위 : 편의성
3순위 : 과학적 고증

# 문서 정책

문서는 **읽는 사람**으로 가른다. 같은 내용을 세 곳에 두지 않는다.

- **게임을 하는 사람 = 인게임뿐.** 물질·특성 설명은 `/guide` 도감(`src/i18n/codex.*.ts`,
  `codexTerms.ts`)과 인게임 툴팁이 **정본**이다. 유저용 설명을 고칠 일이 생기면 여기를
  고친다. 자세한 규칙은 [`docs/CODEX.md`](./docs/CODEX.md).
- **작업자(에이전트·개발자) = 저장소 `docs/` 폴더.** 개발 기록과 상세 레퍼런스는 전부
  여기. 유저에게 보이지 않는 문서이므로 구현 디테일·설계 근거·실패했던 시도를 그대로
  적는다. **하나의 파일이 지나치게 커지지 않도록 분야별로 나눠서** 관리한다(예:
  `ARCHITECTURE.md`, `FEATURES.md`, `PHYSICS.md`, `MATERIALS.md`, `MATERIAL-SYSTEMS.md`,
  `OBJECTS.md`, `TESTING.md`). 새 분야가 생기면 새 파일을 만들고 `docs/README.md`
  인덱스에 링크를 추가한다.
- **Cloudwiki = 개인 포트폴리오 겸 공유 문서.** 유저에게 게임을 설명하는 역할은 인게임
  도감으로 넘어갔다. 이제 이 위키는 **만든 사람이 남에게 보여 주는 기록**이다 — 무엇을
  만들었고 어떻게 굴러가는지. 작업자도 읽지만 정본은 아니므로, 저장소 문서와 어긋나면
  `docs/`가 맞다.
- **이 파일(`AGENTS.md`) = 프로젝트 전반 규칙만.** 개별 물질·오브젝트·계통의
  세부나 코드 디테일은 여기 적지 않고 `docs/`의 해당 분야 문서에 둔다.

# 워크플로우

1. **시작 단계에서 모호하거나 결정이 필요한 사안은 유저에게 질문으로 확인받기.**
   묻는 선은 하나다 — **두 해석이 서로 다른 결과물을 낳는가.** 그렇다면 코드를 쓰기 전에
   묻고, 아니라면 묻지 말고 **가정을 명시한 뒤 진행**한다. 갈리는지 아닌지가 애매할 때만
   되돌림 비용을 보조선으로 쓴다 — 되돌리기 싼 쪽(이름·배치·기본값)은 정하고, 정한 걸
   밝히고, 아니라 하면 그때 바꾼다. **되돌리기 비싼 쪽은 묻는다.**
   - **무엇을 묻나** — 범위(어디까지 건드리나), 방식(무엇을 포기하고 무엇을 얻나),
     완료 조건(무엇을 성공으로 볼지). 취향이 아니라 **갈림길**을 묻는다.
   - **어떻게 묻나** — 한 라운드에 2~4개를 모아 한 번에. 선택지에는 **권장안을 맨 앞에**
     두고 왜 그렇게 보는지 한 줄 붙인다. "이대로 진행할까요" 같은 승인 요청은 질문이 아니다.
   - **언제 묻나** — 착수 전 1라운드가 기본. 작업 중 새 갈림길이 드러나면 그때 다시 묻되,
     **그 답에 걸리지 않는 부분은 먼저 끝내 두고** 묻는다.
2. 새 브랜치에서 작업 수행
3. 작업 후 1차 Push 및 Pull Request 생성
4. 서브에이전트 호출을 통해 코드리뷰
5. 리뷰가 no issues로 통과 시 call_user 도구 사용 / 리뷰에서 지적사항이 있다면 조치후 리뷰 재시도 (no issues 확정 까지 반복)
6. 개발 기록은 `docs/` 폴더의 해당 분야 문서를 갱신(파일이 커지면 분야별로 분할).
   **유저에게 설명이 필요한 변경이면 인게임 도감·툴팁**을 같이 고친다(물질을 추가했다면
   `src/i18n/codex.*.ts`, 새 태그면 `codexTerms.ts` — `npm run test:codex`가 빠진 것을
   이름으로 알려 준다). Cloudwiki 는 **간판이 될 만한 결과물일 때만** 갱신하고, 편집은
   즉시 반영을 권장함.

작업 완료 후 체크인 트리거는 생성하지 않음. 
브라우저 테스트(Playwright 등)는 직접 수행하지 않고 유저에게 요청.

# 스킬 (`.claude/skills/`)

- **`create-svg-assets`** — 픽셀아트/SVG 에셋(오브젝트 스프라이트, 물질 아이콘, 캔버스
  장식)을 만들 때. **파일을 열기 전에** 읽을 것.
- **`add-material-or-object`** — 새 물질 또는 새 독립 오브젝트를 추가할 때의 전 과정
  체크리스트. **코드를 쓰기 전에** 읽을 것.

# 검증

- 전체: `npm test` (아래 검사들 + 스냅샷 파일 + 열 커널 골든 테스트. 열 커널은 `wasm/`
  Rust 빌드 산출물이 필요하므로 없는 환경에선 개별 스크립트로 돌릴 것).
- 물질 id 중복 검사(`npm run check:material-ids`)는 `npm run build`/`deploy`의 첫 단계로
  묶여 있어 **id가 겹치면 Cloudflare Workers 빌드가 강제 실패한다.** 새 물질을 추가하면
  먼저 이걸 돌릴 것.
- 계통별 회귀 테스트 목록과 **"어떤 코드를 건드리면 무엇을 돌려야 하는가"** 는
  [`docs/TESTING.md`](./docs/TESTING.md)에 있다. 엔진·물질·오브젝트를 수정하기 전에
  해당 절을 확인하고, 새 검증 스크립트를 추가하면 그 문서에 절을 만든다.
