# 이 프로젝트는 웹 브라우저 기반 샌드박스 파티클 시뮬레이터임.
1순위 : 재미
2순위 : 편의성
3순위 : 과학적 고증

# 문서 정책

- **Cloudwiki "Web sandbox 프로젝트" 문서 = 소개문서.** 개발 기록을 쌓는 곳이
  아니라, 프로젝트가 무엇이고 무엇을 할 수 있는지 처음 보는 사람에게 소개하는
  가벼운 문서로 유지한다. 개요·핵심 재미·주요 기능 하이라이트·플레이 가이드
  링크·배포 링크 정도만 담고, 세부 변경 이력이나 구현 디테일은 넣지 않는다.
- **개발 기록·상세 레퍼런스 = 저장소 `docs/` 폴더.** 기능·물질·물리 등 상세
  문서는 이곳에 둔다. **하나의 파일이 지나치게 커지지 않도록 분야별로 나눠서**
  관리한다(예: `ARCHITECTURE.md`, `FEATURES.md`, `PHYSICS.md`, `MATERIALS.md`,
  `MATERIAL-SYSTEMS.md`, `OBJECTS.md`). 새 분야가 생기면 새 파일을 만들고 `docs/README.md` 인덱스에
  링크를 추가한다.

# 워크플로우

1. 새 브랜치에서 작업 수행
2. 작업 후 1차 Push 및 Pull Request 생성
3. Sonnet 서브에이전트 호출을 통해 코드리뷰
4. 리뷰가 no issues로 통과 시 call_user 도구 사용 / 리뷰에서 지적사항이 있다면 조치후 리뷰 재시도 (no issues 확정 까지 반복)
5. 개발 기록은 `docs/` 폴더의 해당 분야 문서를 갱신(파일이 커지면 분야별로 분할).
   필요 시 Cloudwiki "Web sandbox 프로젝트" 소개문서의 하이라이트만 최신화한다.
   소개문서는 즉시 반영으로 편집을 권장함.

작업 완료 후 체크인 트리거는 생성하지 않음. 
브라우저 테스트(Playwright 등)는 직접 수행하지 않고 유저에게 요청.

# 스킬 (`.claude/skills/`)

- **`create-svg-assets`** — 픽셀아트/SVG 에셋(오브젝트 스프라이트, 물질 아이콘,
  캔버스 장식)을 만들 때. 색 포맷·저작 형식 3종·해상도와 종횡비 법칙·음영
  하우스 스타일·팔레트 SVG 생성·크기 예산. **파일을 열기 전에** 읽을 것.
- **`add-material-or-object`** — 새 물질 또는 새 독립 오브젝트를 추가할 때의 전
  과정 체크리스트. id 배정과 세이브 호환, 등록·카테고리 배선, i18n, 아이콘 지정,
  형질별 부가 등록, 검증, `docs/`·Cloudwiki 갱신까지. **조용히 실패하는 항목**
  (한국어명 누락, 도감 행 누락 등)을 이 문서가 모아 둔다.

# 검증 스크립트

- **물질 id 중복 검사** — `npm run check:material-ids`
  (내부적으로 `node scripts/check-material-ids.mjs` 실행). `src/game/materials/`
  의 모든 `register({ ... })`에서 `id`를 정적으로 스캔해 중복이 있으면 목록을
  출력하고 종료 코드 1로 실패한다. 중복이 없으면 통과.
  이 검사는 `npm run build`(따라서 `deploy`)의 첫 단계로 묶여 있어,
  **Cloudflare Workers 빌드 시 자동 실행되며 물질 id가 겹치면 빌드가 강제 실패한다.**
  새 물질을 추가할 때는 이 스크립트로 id 충돌 여부를 먼저 확인할 것.

- **전기(스파크) 패킹 검사** — `npm run test:electricity`
  (`test/electricity.ts`). 스파크가 `aux` 16비트에 패킹하는 도체 클래스·펄스 강도가
  서로를 침범하지 않는지 지킨다: 매체별 실측 도달거리(물·염수·산·철), 등록된 전 도체의
  클래스 왕복, 16비트 aux의 세이브/로드 왕복, `auxHi` 없는 구형 세이브 로드까지.
  여기에 **전원 3종의 박자**도 같이 지킨다 — 배선 전용 터빈 출력, 태양광 패널의 배터리
  박자, 그리고 **터빈이 리튬 배터리와 같은 주파수인지**(같은 기하의 실제 배터리와 맞춰
  스파크가 뜬 **틱 번호 목록**을 비교하므로 위상까지 본다). 그중 핵심 회귀 핀은 **너울지는
  증기**: 4틱 중 1틱만 증기를 넣어도 박자가 유지돼야 하고, 증기 틱 세기로 되돌리면 여기서만
  1/4로 처진다(꾸준한 증기 장면은 옛 코드도 통과한다).
  전기 계통(`spark.ts`)이나 `Grid.aux`/저장 포맷, `turbine.ts`/`solarpanel.ts`의 박자를
  건드리면 이걸 돌릴 것.

- **나무 상자 오브젝트** — `npm run test:woodbox` (`test/woodbox.ts`). 나무 상자의
  회전·가연성·파괴 연쇄를 지킨다: 평지 밑변 flush 안착과 수직 낙하 시 각도 유지, **접촉
  토크로 회전**(밀면 구르고 경사에서 굴러 내려감)과 **정지 시 90° 복귀**, **고속 충돌
  파괴**(법선 접근 속도 기준 — 지면 고속 주행은 무사), 물 위 부유율,
  **액체·가루 진입 파티클**(착수/모래 충돌 시 1회성 산개, 가만히 얹으면 없음, 드럼통도 동일),
  지속 가열 점화와 실제 Fire 방출, 다 타면 조각 3개(불 상속), 압사/폭발
  직격도 같은 조각 3개, **파괴 원인별 산개 분기**(충격 = 충돌·폭발·반물질일 때만 조각이
  튀고 톱밥이 흩날림 / 연소·끼임·광선은 제자리에서 무너지고 톱밥도 그 자리에 쌓임),
  조각 파괴 시 톱밥만 남고 추가 오브젝트 없음, 물 진화 후 생존,
  용암은 (액체지만) 진화시키지 않음, **산 용해**(웅덩이에 닿으면 1초 만에 조각 3개 →
  조각도 이어서 녹음, 조각 속도가 상자 속도와 정확히 일치해 안 튐 / 옆에 부은 얇은 산 막이
  흘러와 닿기만 해도 파괴 + 톱밥 잔류 / **대조군 4종**: 같은 웅덩이의 물, 임계 미만 접촉 후
  구출, 언 산, 같은 산 속의 드럼통·고무공). 오브젝트 레이어(`engine/objects.ts`)나 나무 상자
  아트를 건드리면 이걸 돌릴 것.

- **드럼통 파괴 연쇄** — `npm run test:drumbreak` (`test/drumbreak.ts`). 드럼통 3종이 나무
  상자처럼 **조각 3개로 부서지는** 계통을 지킨다: 폭발 직격 시 통 → 조각 3개와 그때 한 번
  쏟아지는 내용물(원유·산, 빈 통은 0셀 대조군), 조각을 다시 부숴야 나오는 철가루와
  **두 번 쏟지 않는 것**, **갓 태어난 조각의 폭발 유예**(없으면 자기를 만든 플래시에 1틱 만에
  삼켜져 기능이 안 보인다 — 유예가 끝난 뒤 새 장약에는 부서지는 것까지), **충돌로는 안
  부서지는 것**(같은 낙하·같은 벽 충돌에서 나무 상자는 터지는 대조군과 나란히 — 폭발물에만
  파괴), **2단 용융**(1200°×24틱에 통이 열려 조각이 되고, 조각은 **더 높은** 1300°×8틱에
  Molten Iron이 되며, 그 사이 1250°에서는 조각만 남는 갈림), 원인별 산개 분기(용융·압사는
  제자리, 폭발은 튀고 각자 스핀), 조각이 고철 밀도라 **가라앉는 것**(통은 뜨는 대조군).
  `breakDrum`·`DRUM_PIECE_*` 상수·`placeShard`나 드럼 조각 아트를 건드리면 이걸 돌릴 것.

- **쌓기 · 오브젝트끼리 충돌 파괴** — `npm run test:stacking` (`test/stacking.ts`). 드럼통·나무
  상자가 **둥근 사각형**으로 충돌하며 생긴 평면 위에 다른 바디가 실제로 쌓이는지, 그리고 속도로
  부서지는 오브젝트가 **그리드가 아니라 다른 오브젝트에 부딪혔을 때도** 부서지는지를 지킨다:
  상자 위 상자가 붙어 서서 완전 정지하는 것, 드럼통 3개가 기둥으로 쌓이는 것, 상자가 드럼통
  뚜껑을 타는 것, **고무공은 여전히 공 위에서 굴러떨어지는 것**(원형 대조군), 아래 상자를 끌면
  위 상자가 마찰로 실려 오는 것, 상자↔상자 충돌이 **양쪽 다** 터뜨리되 임계 아래면 아무것도 안
  부수는 것, 화염병이 상자 위에 떨어져 깨지되 그 상자는 자기 임계에 한참 못 미쳐 멀쩡한 것,
  **드럼통 둘은 같은 속도로 들이받아도 멀쩡한 것**(강철은 폭발물에만 열린다는 대조군).
  `bodyCore`·`gridContacts`·`resolvePair`·`solveNormalPair`·`CONTACT_ROUNDS`를 건드리면 이걸
  돌릴 것.

- **화염병 오브젝트** — `npm run test:molotov` (`test/molotov.ts`). 화염병의 **약한 파괴 임계**와
  **심지 상태 기계**를 지킨다: 스프라이트 12×28 px에서 유도한 캡슐 6×14칸 일치, 바닥에 놓은 병은
  넘어져도 생존하지만 **23칸 낙하는 깨짐**(같은 높이의 나무 상자는 무사 — "더 쉽게"의 대조군),
  **불붙은 파괴는 800°로 태어난 알콜이 실제로 타고** 꺼진 파괴는 20° 알콜에 Fire 0칸,
  **유리 조각은 `launchDebris`로 날아가고 알콜은 제자리에 남는 것**(파괴 그 틱에 유리 0칸 →
  40틱 뒤 착지, 그리고 같은 파괴에서 유리 산개 폭 > 알콜 폭),
  **연못에 빠지면 소화**(같은 연못의 다이너마이트 도화선은 계속 탄다는 대조군)·부유·**꺼진 동안
  연료 보존**·재점화, 심지가 뿌리는 진짜 Fire, **15초 뒤 빈 유리병** 전환과 그 뒤 깨면 유리만
  나오는 것, **자기 불꽃보다 뜨거운 1050° 열탕에서도 안 터지고 1600°에는 터지는 것**(불꽃 자신이
  1000°이므로 이 대조군이 곧 "타는 화염병은 자기를 못 터뜨린다"의 증명이다 — 온도는 저장소가
  아니라 **주변 셀**에 걸어야 한다. 저장소 직접 대입은 판정 직전 같은 틱에 주변으로 이완돼
  1050°가 ≈968°로 읽힌다), 폭발 직격. 오브젝트
  레이어(`engine/objects.ts`)나 `render/molotovSprite.ts`, 공용 소화 판정 `bodyQuenchFrac`를
  건드리면 이걸 돌릴 것.

- **불꽃놀이 꽃** — `npm run test:fireworks` (`test/fireworks.ts`). 자탄이 꽃(Firework
  Burst)을 피우는 것, 꽃 셀이 실온 20°가 아니라 1200°로 읽히는 것, 돋보기가 그 온도를
  보고하는 것, 그리고 그 온도가 **표시용일 뿐**이라 꽃이 스친 나무·나무 상자·꺼진
  다이너마이트를 데우거나 불붙이거나 되살리지 않는 것(`Material.decorTemp`)을 지킨다.
  불꽃놀이 계통이나 decorTemp를 건드리면 이걸 돌릴 것.

- **알루미늄 계통** — `npm run test:aluminum` (`test/aluminum.ts`). 알루미늄의 **용융/연소
  갈림**(불꽃이 닿으면 타고 그냥 데우면 660°에 녹음, 타는 중인 알갱이는 용융으로 안 빠짐),
  **주조 왕복**(가루 → Molten Aluminum → 고체 Aluminum → 다시 용융)과 **쇳물이 실제로 흐르는지**
  (기둥이 무너져 옆으로 퍼짐 — 초기 구현이 `updateLiquid` 호출을 빠뜨려 제자리에 굳던 버그의
  회귀 검사), 고체 알루미늄의 **스파크 전도·레이저 거울·열전도 우위·비자성**,
  **Aluminum Powder + Saltpeter → Flash Powder** 레시피와 150° 찬 가루 게이트, 테르밋 레시피
  무회귀, 섬광화약의 200° 자연발화·젖음 불발·전기 기폭, 섬광화약 폭발이 **돌·철을 한 칸도 못
  부수는지**(파괴력 6)와 **원반이 주황 Blast가 아니라 백색 Flash 빛으로 차는지**(Flash가
  가열하지 않고·폭발에 안 밀리고·기폭 트리거가 아닌 것 포함), 그리고 **액체 갈륨이 고체
  알루미늄을 가루로 취화시키되 자신은 소모되지 않고 얼리면 멈추는지**를 지킨다.
  3차 라운드분도 같은 파일에서 지킨다: **갈륨이 가루를 활성 알루미늄으로 바꾸고**(역시
  무소모), **활성 가루가 물을 수소로 바꾸며 웅덩이를 데우고 스스로 소모되는지**(+ 원본
  가루는 물에서 무반응이라는 대조, 그리고 활성 가루도 660°에 녹아 회수되는지 — 자연발화점이
  융점 위에 남아 있어야 성립한다), **산 + 가루/고체 막대의 수소**, **타는 알루미늄 + 수증기의
  수소**(찬 알루미늄은 무반응 + 나온 수소가 자기 자연발화점 200°를 넘는지), **부유 구름의 분진
  폭발**(활성 가루 구름도 같이)**과 같은 불 아래 쌓인 더미·액체에 잠긴 가루는 무사**한지,
  그리고 **암모날 레시피·150° 게이트·프릴보다 큰 크레이터·불타는 크레이터·젖음 불발**까지.
  알루미늄 계통(`aluminumdust.ts` 포함)·갈륨·`spark.ts`의 도체 등록을 건드리면 이걸 돌릴 것.

- **플라스틱 중합 라인** — `npm run test:plastic` (`test/plastic.ts`). 증류의 후속인
  분해→중합 사슬을 지킨다: **크래킹 조건**(촉매 접촉이 유일한 조건 — 1000°로 200틱을
  달궈도 촉매가 없으면 무반응, 상온 20°에서도 촉매에 닿으면 분해. 폐기된 850° 온도
  게이트가 되살아나지 않게 하는 회귀 핀), 유분별 수율(나프타 85% > 등유 60% > 경유 40%),
  1000°에서 에틸렌 무발화 및 불꽃 접촉 시 폭연, 1250° 과열 코킹, 중합 온도창(40~200°)
  상하한, **발열에 의한 자기 정지와 냉각 재개**, 사슬 세대 총량 한계와 범위 밖 `aux`
  클램프, 그리고 **문서에 적힌 빌드의 실제 회수율**(가열 휘발유 웅덩이 → 증기 → 촉매
  지붕. 열은 철 열판에만 쓰고 원료·산물 온도는 건드리지 않는다. 20칸 중 12~19칸 관측,
  시드 없는 난수라 흔들림). 회수율로 보는 이유는 옛 검사가 "에틸렌이 한 칸이라도
  생겼는가"만 보다가 회수율 0%를 통과시켰기 때문이다 — `docs/PETROCHEMISTRY.md` 2.1 참고.
  `petroleumvapor.ts`·`ethylene.ts`·`polyethylene.ts`나
  중합 온도/발열 상수를 건드리면 이걸 돌릴 것.

- **전자석 ↔ 오브젝트 인력** — `npm run test:magnetobjects` (`test/magnetobjects.ts`). 통전된
  전자석이 **강철 바디만**(드럼통 전 종류·연막탄 캔) 끌어당겨 슬래브에 매달고 놓지 않는 것,
  고무공·나무 상자·다이너마이트는 건드리지 않는 것, 무전원·석재 차폐·사거리 밖에서는 전혀
  당기지 않는 것을 지킨다. `objects.ts`의 `applyMagnetPull`이나 `Grid.magnetFields` 발행을
  건드리면 이걸 돌릴 것.

- **열선 ↔ 오브젝트 가열** — `npm run test:heatrayobjects` (`test/heatrayobjects.ts`). Laser의
  열선이 **오브젝트를 데우고 거기서 멈추는** 계통을 지킨다: 명중한 바디의 열 저장소가 실제로
  오르는 것(+ 레이저를 끈 대조군), **물체 뒤가 그늘이 되는 것**(같은 레인의 뒤쪽 기둥 온도를
  물체 유무로 비교 — "빔이 사라졌다"가 "애초에 안 왔다"로 오독되지 않게 대조군이 기하를
  공유한다), 물고 있으면 종류별로 실제 종말(드럼통 용융 + Molten Iron, 나무 상자 점화 →
  소멸 + Fire/Sawdust, 고무공 소멸, 다이너마이트 유폭 + 크레이터), **빔을 끊으면 식고
  살아남는 것**(즉사가 아니라 램프라는 회귀 핀), 유리판 안의 빔을 흡수해도 **유리가
  보존되는 것**(`aux` 복원), 드래그 중 바디는 흡수하지 않는 것. `heatray.ts`의
  `absorbHeatRayCell`이나 `objects.ts`의 `footprintHazards`를 건드리면 이걸 돌릴 것.

- **연막탄 수중 연기** — `npm run test:smokebomb` (`test/smokebomb.ts`). 잠긴 연막탄이 **액체
  속에서도** 연기를 내는 것(도화선·분출 단계 모두), 그 연기가 액체를 **위로 밀어낼 뿐 삭제하지
  않는 것**(물 총량 완전 보존), 언 액체와 밀폐된 만수 수조에서는 생기지 않는 것, 공기 중
  동작 무회귀를 지킨다. `objects.ts`의 `puffSmoke`/`displaceLiquidUp`을 건드리면 이걸 돌릴 것.

- **산 + 금속 수소 발생** — `npm run test:acidmetal` (`test/acidmetal.ts`). 산이 **이온화 경향이
  수소보다 큰 금속**을 녹일 때만 그 산 칸이 수소 거품이 되는 것을 지킨다(`Material.acidHydrogen`
  태그 + `corrosion.ts` 공용 부식 패스): 거품을 내는 8종(철·철가루·알루미늄 3종·우라늄 2종·고체 갈륨)과
  **안 내는 6종**(구리 전선·수은·녹·녹가루·철광석·석회암, 그리고 아예 안 녹는 니크롬)의 대조,
  확률 등급이 곧 반응성 서열이라는 것(수치 직접 비교 — 이웃 확률은 난수로 못 가른다), 가루 >
  막대, 거품이 자연발화점(200°) 아래에서 태어나는 것, **산이 1:1로 소모돼 발생량이 웅덩이
  크기에 묶이는 것**, 액체는 부식 대상이 아니라 액체 갈륨은 무사하고 굳히면 다시 나는 것,
  그리고 **나트륨만은 태그가 아니라 물과 같은 격렬 경로**(뿌리면 불, 뭉치면 폭발)를 타는 것까지.
  여기에 **태그 명단 자체를 레지스트리에서 훑어 비교하는 검사**가 하나 더 있다 — 물질 id
  중복 검사와 같은 발상으로, 금속을 새로 태그하거나 확률을 조정하면 **이 검사가 먼저 깨지면서
  새 명단을 출력**한다. 명단·순위를 손으로 옮겨 적은 주석과 문서(`acid.ts`·`corrosion.ts` 헤더,
  `acidHydrogen` JSDoc, MATERIALS.md 사슬, MATERIAL-SYSTEMS.md 등급 표)가 그때 같이
  갱신돼야 한다는 알림이다 — 리뷰에서 세 번 어긋난 게 정확히 그 손으로 옮긴 명단이었다.
  `corrosion.ts`(Acid·Acid Vapor·Acid Slime이 공유하는 부식 패스)나 `acidHydrogen` 태그를
  건드리면 이걸 돌릴 것.

- **방사선 피폭** — `npm run test:radiation` (`test/radiation.ts`). 방사능 카테고리 전 물질이
  주변 생명 물질을 죽이는 계통을 지킨다: **커버리지**(레지스트리를 훑어 방사능 전 물질이
  `Material.radiation` 선량을, 생명 전 물질이 `radiationDeath` 사체나 `radiationHit` 자체
  모델을 선언했는지 — 예외는 Nanobot·Slime·Acid Slime 3종뿐), **등록된 전 선원이 실제로
  죽이는지**(열로 죽는 조건이 아예 없는 Seed를 프로브로 써서 차가운 U238과 1600° 코륨을 같은
  장면으로 잰다. 모든 장면은 선원을 전도율 0인 Wall 주머니에 가둬 열·증기 오염을 배제한다),
  **무엇이 막고 무엇이 통과하는가**(1칸 터널에 선원 → 시험 물질 3칸 → 식물을 세우고 가운데
  3칸만 바꿔 가며: 공기·모래·원유 통과, 돌 차단 — 기하가 같으므로 "뚫었다"가 "붙어 있었다"로
  오독되지 않고 돌 장면이 대조군이 된다), **사거리**(끝 6칸은 죽고 8칸은 산다. 이 장면의 선원은
  **핵폐기물**이어야 한다 — U238 6칸 막대는 자기들끼리 임계에 들어 녹아 흘러가 버려서 선원이
  놓아둔 자리에 없다), **물질별 사체**(죽는 그 틱에 자리를 읽는다), **방사선 내성 3종**,
  **산호는 즉사가 아니라 백화**(+ 선원만 돌로 바꾼 대조군), **선원 사거리 안에선 백화 골격이
  재군체화하지 않음**(+ 대조군). `engine/radiation.ts`·`Simulation.updateCell`의 선량 구동이나
  세 태그를 건드리면 이걸 돌릴 것.

- **염소 + 나트륨** — `npm run test:chlorinesodium` (`test/chlorinesodium.ts`). 2Na + Cl₂ → 2NaCl,
  즉 **불꽃 + 소금**을 지킨다: 물·공기·점화원을 공으로 돌릴 수 없게 **밀폐 Wall 주머니** 안에서
  일어나는 단일 알갱이 전환(+ 주머니를 비워 둔 대조군), 흩뿌린 가루의 **완전 전환과 정확한
  1:1**(알갱이 수 = 소금 칸 수 — 염소는 촉매가 아니라 함께 소모되는 시약이다), 그리고 **뭉친
  더미도 끝까지 타는 것**(잔존 ≤2 — 자기 휘젓기 충격파를 빼면 24에서 멎던 자리다)과 소모 알갱이
  수 = 소금 칸 수 항등식. 충격파(`stirShock`)는 **되는 것과 안 되는 것을 한 장면에** 세운다:
  같은 거리의 **모래는 흩어지되(파동이 실제로 도달했다는 양성 대조) 돌 기둥은 멀쩡**, 전 구간
  **Blast 0칸**(= 폭약을 섬광으로 태우지 않는다는 증명 — 나트륨 자신이 매 타격 진앙에 있는
  `explosive` 프로브다), **홀알갱이는 안 때리는 것**(바닥의 모래 한 칸이 제자리), 그리고
  **고체 무영향이 완전한 것** — 맞닿은 유리가 안 깨지고(`DetonateOptions.shatters: false` —
  진짜 폭발·우퍼는 기본값 true로 그대로 깬다), 충격파가 보통 실어 나르는 **`shockLoose`
  고체(나노봇 프로브 — 염소가 안 죽이고 열사·충격사도 없는 유일한 물질)가 한 번도 사라지지
  않으며**, 일반형으로 **날아다니는 Debris가 고체를 실어 나른 적이 없는 것**까지 훑는다. 여기에 **폭발이
  아니라 연소라는 갈림**(같은 더미를 물에 담근 대조군은 Blast 발생 — 나중에 두 경로를 "정리"하려는
  수정이 여기서 먼저 깨진다), 반응열이 **소금 융점 800° 아래**라 Molten Salt가 안 생기는 것,
  그리고 같은 이웃 루프를 공유하는 **생물 살상 경로의 무회귀**.
  `chlorine.ts`의 `updateChlorine`·`stirShock`이나 반응 온도 상수를 건드리면 이걸 돌릴 것.

- **반물질 전면 소멸** — `npm run test:antimatter` (`test/antimatter.ts`). 반물질이 **Wall·Void를
  뺀 모든 물질을 지우는 것**을 지킨다. 핵심은 **레지스트리 훑기**다 — 등록된 전 물질을 그레인
  옆에 세워 보고 살아남는 게 있으면 **그 이름을 출력하며 실패**하므로, 나중에 누가 조용히 면제
  조건(방폭·무적·기체 따위)을 되살리면 이 검사가 먼저 깨진다. 나머지는 예외 둘과 섬광 표식이다:
  Wall·Void 생존과 **Void가 오히려 그레인을 삼키는 대조군**, 기체(Steam)의 1:1 소멸과 1200°
  섬광, **표식 붙은 Fire/Smoke는 건너뛰고 평범한 Fire/Smoke는 소멸시키는 짝 장면**(같은 기하,
  차이는 `aux` 표식 하나뿐), 실제 틱 루프에서 Diamond·Clone이 갉여 나가는 장면, 그리고
  자기잠식 방지의 못인 **효율 장면**(밀폐 통로에서 그레인 36개 → 소모 그레인 수 ≈ 파괴 칸 수,
  기준 0.9배 — 표식이 사라지면 이 비율이 먼저 무너진다). `antimatter.ts`의 이웃 판정이나
  `FLASH_MARK`을 건드리면 이걸 돌릴 것.

- **겹침(스며든 액체)** — `npm run test:overlap` (`test/overlap.ts`). 가루 속에 스며든
  액체가 **지워지지 않고**, **상호작용도 한다**는 두 가지를 지킨다. 충격파 쪽: 젖은 모래를
  약한 폭발로 밀어내도 물 총량이 그대로고(파편이 겹침을 업고 난다 — `Material.overlapCarrier`),
  착지 후에도 젖은 채로 남으며, 강한 폭발로 부수면 그 자리에서 **수증기**가 난다(마른 모래
  대조군은 0칸). 상호작용 쪽 장면은 전부 **벽으로 밀폐한 데다 빈틈 없이 꽉 채운 주머니**다 —
  알갱이가 떨어질 틈도, 스며든 액체가 표면화할 빈칸도 없어야 "안에서 물었다"가 "새어 나와
  물었다"로 오독되지 않는다(처음 쓴 판은 벽과 더미 사이에 1칸이 떠 있어서 고치기 전에도
  통과했다). 그 주머니에서 스며든 산이 모래를 녹이고(깨진 유리 대조군은 무사), 철가루에선
  수소가 나고, **1차 셀 물이 0칸인데도** 활성 알루미늄이 수소를 내고(선언형 반응 테이블이
  겹침 너머로 물린다), 시멘트가 스며든 물로 굳는다. 무회귀 핀 둘: **ANFO**(질산암모늄에
  스민 경유는 반응 상대가 없으니 그대로 남아야 한다)와 **배수**(상호작용 패스가 이동 턴을
  잡아먹지 않는다). 모든 장면이 (host, overlay) 불변식을 함께 훑는다.
  `SimContext`의 겹침 판정·`reactions.ts`의 `tryReactSoaked`·`corrosion.ts`의
  `tryCorrodeSoaked`·`blast.ts`의 `resolveSoaked`를 건드리면 이걸 돌릴 것.

- **활성 타일 스캔 동등성** — `npm run test:active-tiles` (기존). `npm test`는
  위 검사들과 스냅샷 파일·열 커널 골든 테스트를 함께 돌린다(열 커널은 `wasm/` Rust 빌드 산출물이
  있어야 하므로, 그게 없는 환경에선 개별 스크립트로 돌릴 것).
