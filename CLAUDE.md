# 이 프로젝트는 웹 브라우저 기반 샌드박스 파티클 시뮬레이터임.
1순위 : 재미
2순위 : 편의성
3순위 : 과학적 고증

# 문서 정책

- **Cloudwiki "Web sandbox 프로젝트" 문서 = 소개문서.** 개발 기록을 쌓는 곳이
  아니라, 프로젝트가 무엇이고 무엇을 할 수 있는지 처음 보는 사람에게 소개하는
  가벼운 문서로 유지한다. 개요·핵심 재미·주요 기능 하이라이트·플레이 가이드
  링크·배포 링크 정도만 담고, 세부 변경 이력이나 구현 디테일은 넣지 않는다.
- **개발 기록·상세 레퍼런스 = 저장소 `docs/` 폴더.** 기능·물질·물리 등 상세
  문서는 이곳에 둔다. **하나의 파일이 지나치게 커지지 않도록 분야별로 나눠서**
  관리한다(예: `ARCHITECTURE.md`, `FEATURES.md`, `PHYSICS.md`, `MATERIALS.md`,
  `MATERIAL-SYSTEMS.md`, `OBJECTS.md`). 새 분야가 생기면 새 파일을 만들고 `docs/README.md` 인덱스에
  링크를 추가한다.
- **이 파일(`CLAUDE.md`) = 프로젝트 전반 규칙만.** 개별 물질·오브젝트·계통의
  세부나 코드 디테일은 여기 적지 않고 `docs/`의 해당 분야 문서에 둔다.

# 워크플로우

1. **시작 단계에서 모호하거나 결정이 필요한 사안은 `AskUserQuestion`으로 확인받기.**
   해석이 갈리면 결과물이 달라지는 지점(범위·방식·트레이드오프)은 코드를 쓰기 전에 묻는다.
2. 새 브랜치에서 작업 수행
3. 작업 후 1차 Push 및 Pull Request 생성
4. Sonnet 서브에이전트 호출을 통해 코드리뷰
5. 리뷰가 no issues로 통과 시 call_user 도구 사용 / 리뷰에서 지적사항이 있다면 조치후 리뷰 재시도 (no issues 확정 까지 반복)
6. 개발 기록은 `docs/` 폴더의 해당 분야 문서를 갱신(파일이 커지면 분야별로 분할).
   필요 시 Cloudwiki "Web sandbox 프로젝트" 소개문서의 하이라이트만 최신화한다.
   소개문서는 즉시 반영으로 편집을 권장함.

작업 완료 후 체크인 트리거는 생성하지 않음. 
브라우저 테스트(Playwright 등)는 직접 수행하지 않고 유저에게 요청.

# 스킬 (`.claude/skills/`)

- **`create-svg-assets`** — 픽셀아트/SVG 에셋(오브젝트 스프라이트, 물질 아이콘, 캔버스
  장식)을 만들 때. **파일을 열기 전에** 읽을 것.
- **`add-material-or-object`** — 새 물질 또는 새 독립 오브젝트를 추가할 때의 전 과정
  체크리스트. **코드를 쓰기 전에** 읽을 것.

# 검증

- 전체: `npm test` (아래 검사들 + 스냅샷 파일 + 열 커널 골든 테스트. 열 커널은 `wasm/`
  Rust 빌드 산출물이 필요하므로 없는 환경에선 개별 스크립트로 돌릴 것).
- 물질 id 중복 검사(`npm run check:material-ids`)는 `npm run build`/`deploy`의 첫 단계로
  묶여 있어 **id가 겹치면 Cloudflare Workers 빌드가 강제 실패한다.** 새 물질을 추가하면
  먼저 이걸 돌릴 것.
- 계통별 회귀 테스트 목록과 **"어떤 코드를 건드리면 무엇을 돌려야 하는가"** 는
  [`docs/TESTING.md`](./docs/TESTING.md)에 있다. 엔진·물질·오브젝트를 수정하기 전에
  해당 절을 확인하고, 새 검증 스크립트를 추가하면 그 문서에 절을 만든다.
